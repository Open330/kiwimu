{
  "name": "kiwimu",
  "version": "0.2.0",
  "description": "Turn textbooks and web content into your own interlinked wiki",
  "type": "module",
  "bin": {
    "kiwimu": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target bun",
    "test": "bun test"
  },
  "dependencies": {
    "cheerio": "^1.0.0",
    "commander": "^12.1.0",
    "gh-pages": "^6.3.0",
    "marked": "^15.0.0",
    "smol-toml": "^1.3.1",
    "turndown": "^7.2.0"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "@types/turndown": "^5.0.5"
  }
}
